# 🌤️ Weather App

Simple weather application with Express.js and OpenWeatherMap API.

## 🚀 Quick Start

1. **Install dependencies**
   ```bash
   npm install
Create .env file

env
OPENWEATHER_API_KEY=your_api_key_here
PORT=3000
Run the app

bash
npm run dev
Open browser
http://localhost:3000

📁 Project Structure
server.js - main server

views/ - EJS templates

public/css/ - styles

.env - environment variables

text

Або навіть ще коротше:

```markdown
# Weather App

```bash
npm install
npm run dev
Visit: http://localhost:3000
